server:
  port: 8081
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GuerraS/store-project
          search-paths: config-data
          username: ${GUT_USER}
          password: ${GIT_PASSWORD}
          